SET(TARGET_SRC bitcoind.cpp)

SET(TARGET_ADDED_LIBRARIES coinWallet coinMine coinNAT)

SET(TARGET_EXTERNAL_LIBRARIES
    ${CMAKE_THREAD_LIBS_INIT}    
    ${MATH_LIBRARY} 
    ${OPENSSL_LIBRARIES} 
    ${Boost_LIBRARIES} 
    ${BDB_LIBRARY} 
    ${SQLITE3_LIBRARIES}
    ${DL_LIBRARY}
)

SETUP_APPLICATION(bitcoind)
