SET(TARGET_SRC jsonance.cpp)

IF( CMAKE_SYSTEM MATCHES "Linux" )
    SET(TARGET_EXTERNAL_LIBRARIES
        ${CMAKE_THREAD_LIBS_INIT}    
        ${MATH_LIBRARY} 
        ${OPENSSL_LIBRARIES} 
        ${Boost_LIBRARIES} 
        ${DL_LIBRARY}
        ${RT_LIBRARY}
    )
ELSE( CMAKE_SYSTEM MATCHES "Linux" )
    SET(TARGET_EXTERNAL_LIBRARIES
        ${CMAKE_THREAD_LIBS_INIT}    
        ${MATH_LIBRARY} 
        ${OPENSSL_LIBRARIES} 
        ${Boost_LIBRARIES} 
        ${DL_LIBRARY}
    )
ENDIF( CMAKE_SYSTEM MATCHES "Linux" )

SETUP_TEST(jsonance)
