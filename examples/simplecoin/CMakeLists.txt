SET(TARGET_SRC simplecoin.cpp)

SET(TARGET_ADDED_LIBRARIES coinWallet)

SET(TARGET_EXTERNAL_LIBRARIES
    ${CMAKE_THREAD_LIBS_INIT}    
    ${MATH_LIBRARY} 
    ${OPENSSL_LIBRARIES} 
    ${Boost_LIBRARIES} 
    ${BDB_LIBRARY} 
    ${DL_LIBRARY}
)

SETUP_EXAMPLE(simplecoin)
