SET(TARGET_SRC ponzicoin.cpp)

SET(TARGET_ADDED_LIBRARIES coinWallet coinPool)

SET(TARGET_EXTERNAL_LIBRARIES
    ${CMAKE_THREAD_LIBS_INIT}    
    ${MATH_LIBRARY} 
    ${OPENSSL_LIBRARIES} 
    ${Boost_LIBRARIES} 
    ${BDB_LIBRARY} 
    ${SQLITE3_LIBRARIES}
    ${DL_LIBRARY}
)

SETUP_EXAMPLE(ponzicoin)
